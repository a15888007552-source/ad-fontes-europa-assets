# Ad Fontes Europa static image cache

This repository stores the static Wikimedia/Wikipedia image cache used by the Europa map in the `ad-fontes` repository.

Every cached item is listed in [`manifest.json`](./manifest.json) with its source page, source image URL, and any license metadata returned by the Wikimedia API. A blank or unknown license field still requires manual attribution review.
